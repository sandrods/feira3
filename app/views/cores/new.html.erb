<% content_for :title do %>
	<h1><strong>Nova Cor</strong></h1>
<% end %>

<%= error_messages_for :cor %>

<%= form_for(:cor, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>

	<div class="buttons clear">
		<%= f.submit 'Salvar e Nova' %> <%= f.submit 'Salvar' %> ou <%= link_to 'Voltar para Lista', collection_url, :class=>'link' %>
	</div>
<% end %>
