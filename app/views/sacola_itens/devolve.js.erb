<%-if @err_msg -%>

	beep_error();
 	alert(<%= @err_msg %>)

<% else %>

	$('#dev_title').show();

	$('#lista_incluidos').html("<%= escape_javascript(render(:partial => 'item', :collection => @sacola.itens.incluidos)) %>");
	$('#lista_devolvidos').html("<%= escape_javascript(render(:partial => 'item', :collection => @sacola.itens.devolvidos)) %>");

	$('#totais_tr').html("<%= escape_javascript(render(:partial => 'totais_tr')) %>");

  $('#barcode1').val('');

	box({nome: '<%= @item.item.produto.ref %>',
        cor: '<%= @item.item.cor.nome %>',
        tamanho: '<%= @item.item.tamanho.nome %>'
  });

<%- end -%>