<%-if @err_msg -%>

	beep_error();
 	alert(<%= @err_msg %>)

<% else %>

	$('#dev_title').show();

	$('#lista_incluidos').html("<%= render_partial_js('item', :collection => @sacola.itens.incluidos) %>");
	$('#lista_devolvidos').html("<%= render_partial_js('item', :collection => @sacola.itens.devolvidos) %>");

	$('#totais_tr').html("<%= render_partial_js('totais_tr') %>");

  $('#barcode1').val('');

	box({nome: '<%= @item.item.produto.ref %>',
        cor: '<%= @item.item.cor.nome %>',
        tamanho: '<%= @item.item.tamanho.nome %>'
  });

<%- end -%>